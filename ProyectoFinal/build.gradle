apply plugin: 'java'
apply plugin:'application'
mainClassName = "lifegame.JuegoVida"
dependencies {
    // Dependency on local binaries
    implementation fileTree(dir: 'libs', include: ['core.jar'])
}
